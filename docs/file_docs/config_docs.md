# Own docs for config.py script

- Central config for topics, pathing and constants that are "immutable" in that way that they belong in config.py and will not move anywhere else. If they are needed, they will be imported.